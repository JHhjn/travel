<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hniu.travel.mapper.PermissionMapper">
    <insert id="addPermissionByRid">
        insert into role_permission values (#{rid},#{pid})
    </insert>
    <delete id="delByRid">
        delete from role_permission
        where role_permission.rid=#{rid}
    </delete>

    <select id="findPermissionByRid" parameterType="integer" resultType="integer">
        select permission.pid
        from role_permission left join permission
            on role_permission.pid=permission.pid
        where role_permission.rid=#{rid}
    </select>
</mapper>