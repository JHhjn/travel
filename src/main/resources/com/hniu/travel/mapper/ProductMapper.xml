<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hniu.travel.mapper.ProductMapper">
    <resultMap id="productMapper" type="com.hniu.travel.pojo.Product">
        <id property="pid" column="pid"></id>
        <result property="productName" column="productName"></result>
        <result property="price" column="price"></result>
        <result property="status" column="status"></result>
        <result property="hotline" column="hotline"></result>
        <result property="productDesc" column="productDesc"></result>
        <result property="pImage" column="pImage"></result>
        <result property="cid" column="cid"></result>
        <association property="category" column="cid" javaType="com.hniu.travel.pojo.Category">
            <id property="cid" column="cid"></id>
            <result property="cname" column="cname"></result>
        </association>
    </resultMap>
    <insert id="add">
        insert into favorite values(#{mid},#{pid})
    </insert>
    <delete id="del">
        delete from favorite where  mid=#{mid} and pid=#{pid}
    </delete>
    <select id="findProductPage" resultMap="productMapper">
        SELECT *
        FROM product
                 LEFT JOIN category ON product.cid = category.`cid`
        order by product.pid desc
    </select>

    <select id="findOne" parameterType="integer" resultMap="productMapper">

          select *from product left join  category on  product.cid= category.cid
            where product.pid=#{pid}
    </select>
    <select id="findMidHasPid" resultType="java.lang.Integer">
        select * from favorite where mid=#{mid} and pid=#{pid}
    </select>
    <select id="findMemberFavorite" resultType="com.hniu.travel.pojo.Product">
        SELECT product.*
        FROM product
                 LEFT JOIN favorite
                           ON product.`pid` = favorite.`pid`
        WHERE favorite.`mid` = #{mid}
    </select>
    <select id="findTour" resultType="com.hniu.travel.pojo.Product">
        SELECT
            *
        FROM
            product
        WHERE
            product.productName LIKE '%团%' AND
            product.productName NOT LIKE '%节%'
        ORDER BY
            product.price DESC
            LIMIT #{random},5
    </select>
</mapper>